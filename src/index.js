import React from 'react';
import ReactDOM from 'react-dom';
// This is the starting point where we create our frontend React app, App.js
import App from './App';

// this is the main thing that renders our app
// it will render our App and put it 'root'
ReactDOM.render(<App />, document.getElementById('root'));
